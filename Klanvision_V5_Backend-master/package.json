{
  "name": "klanvision-backend",
  "version": "1.0.0",
  "description": "Klanvision backend – Cloudflare Workers + Hyperdrive (MySQL)",
  "main": "src/index.js",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "itty-router": "^5.0.18",
    "qrcode-svg": "^1.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
